# Семинар 5

## Указатели и константность

```cpp
T* const var    // константынй указатель - меняем значение, не меняем адрес
const T* var    // указатель на константу - меняем адрес, не меняем значение
```


## Про память
```
┌─────────────────────────┐ Высокие адреса
│    Стек (Stack)         │
│  - локальные переменные │
│  - параметры функций    │
│                         │
├─────────────────────────┤
│                         │
│    Свободная память     │
│                         │
├─────────────────────────┤
│    Куча (Heap)          │
│  - динамическая память  │
│  - выделяется через new │
├─────────────────────────┤
│  ...                    │
├─────────────────────────┤
│  ...                    │
├─────────────────────────┤
│  ...                    │
└─────────────────────────┘ Низкие адреса
```

